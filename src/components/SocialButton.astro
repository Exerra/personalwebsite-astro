---
const { href, label, icon } = Astro.props
---

<button aria-label={label}>
    <a href={href} target={"_blank"} class={"fill-gray-500 hover:fill-selected-text"}>
        <slot />
    </a>
</button>