---
const { href, label, icon } = Astro.props
---

<button aria-label={label}>
    <a href={href} aria-label={label} target={"_blank"} class={"transition ease-in-out fill-black/60 hover:fill-black hoverable"}>
        <slot />
    </a>
</button>