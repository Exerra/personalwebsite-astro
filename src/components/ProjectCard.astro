---
const { date, name, description, href, label } = Astro.props
---

<div class={"w-full basis-1/1 grow lg:basis-1/3 shadow-2xl bg-beige text-grey-card rounded-[35px] p-6 flex flex-col justify-between"}>
    <div>
        <h2 class="text-4xl lg:text-5xl font-bold">{name}</h2>
        <hr class="border-2 border-grey-card rounded-xl my-3" />
        <p class="text-xl mb-4" set:html={description} />
    </div>
    <a href={href} target={"_blank"} class={"transition ease-in-out text-grey-card/80 text-xl font-bold hover:text-grey-card after:content-['_↗']"}>{label}</a>
</div>